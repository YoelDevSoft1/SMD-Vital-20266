# Polish End-to-End del PWA — 2026-07-06

> Resumen del polish visual ejecutado en paralelo por 4 agentes (1 foundation + 3 paralelos) sobre `admin-panel-frontend/`.

## Resumen ejecutivo

**Total de archivos modificados:** 28 archivos (10 foundation + 18 polish).
**Build:** ✅ pasa sin errores (warnings pre-existentes: chunk size >500kb, tsconfig extends astro base).
**Cambios totales:** ~720 líneas agregadas, ~407 líneas removidas (de 28 archivos).

## Fase 0 — Foundation (1 agente secuencial, merge en `44cbe16`)

**Branch:** `feat/foundation-design-system`

**Archivos (10):**
- `src/components/ErrorBoundary.tsx` (crear) — class component con fallback y acciones Reintentar/Inicio/Recargar
- `src/components/RealtimeIndicator.tsx` (crear) — badge "En vivo" + dot animado que aparece 4s tras cada evento Socket.IO
- `src/main.tsx` — envolver QueryClientProvider en `<ErrorBoundary>`
- `src/App.tsx` — montar `<RealtimeIndicator />` globalmente
- `src/services/realtime.service.ts` — emite `CustomEvent('smd:realtime-update')` cuando llega evento socket
- `src/components/ui/Boton.tsx` — `sm` 36→40px, `icon` 40→44px, nuevo `iconSm` 36px desktop
- `src/components/ui/Entrada.tsx` — `text-base` + `min-h-[44px]` (iOS zoom-safe)
- `src/components/ui/Seleccion.tsx` — `text-base` + `min-h-[44px]`
- `src/components/ui/Alerta.tsx` — nueva prop `sticky` con safe-area-inset-top
- `src/index.css` — removida regla global `input { font-size: 16px }` y `overscroll-behavior-y: none`

**Problemas críticos resueltos:**
1. ✅ Sin `ErrorBoundary` global → app puede quedar en blanco. AHORA capturada con fallback accesible.
2. ✅ `index.css` ocultaba bug de `text-sm` en inputs (regla global). AHORA cada componente es responsable.

## Fase 1 — Tres workstreams en paralelo

### WS-A: Mobile roles (commit `ff94457`, `d1b8f76`)
**Branch:** `feat/mobile-roles`

**Archivos (6):**
- `src/pages/Login.tsx` — inputMode correcto, polish visual
- `src/pages/Register.tsx` — inputMode correcto, polish visual
- `src/pages/PatientHistory.tsx` — loading skeleton con `EsqueletoLista`, empty state rico
- `src/pages/DoctorDashboard.tsx` — error→`<Alerta>`, skeleton con `<EsqueletoLista>`, empty states con `<EstadoVacio>`, `<Insignia>` para status, sticky aria-label
- `src/pages/MyCommissions.tsx` — Boton top usa variant por defecto (sin `bg-slate-950` raw)
- `src/pages/MyEarnings.tsx` — **bug fix**: `useMemo` → `useEffect` para reset reason en DisputeModal

### WS-B: Admin core + Forms (commit `58da5ef`)
**Branch:** `feat/admin-core-forms`

**Archivos (12):**
- `src/pages/Users.tsx` — touch targets 44px en botones de acción, dark mode coherente
- `src/pages/Doctors.tsx` — empty state con `EstadoVacio icon={Stethoscope}`, touch targets 44px
- `src/pages/Appointments.tsx` — empty state con `Calendar`, grid-cols-2 mobile para acciones
- `src/pages/Services.tsx` — **+30 ocurrencias dark:** (era 0), empty state, mobile-first
- `src/pages/Reviews.tsx` — empty state con `MessageSquare`, skeleton, header flex-col-responsive
- `src/components/CreateUserForm.tsx` — `<select>` rol con text-base + dark, footer safe-area
- `src/components/EditUserForm.tsx` — mismas mejoras
- `src/components/CreateDoctorForm.tsx` — bug i18n corregido, contraseña unificada a 8 chars, tokens dark
- `src/components/EditDoctorForm.tsx` — tokens dark, footer safe-area
- `src/components/CreatePaymentForm.tsx` — bug i18n, spinner custom→`Boton isLoading`, dark tokens
- `src/components/CreateServiceForm.tsx` — bug i18n "El firstName del servicio es required" → "El nombre del servicio es requerido"
- `src/components/CreateReviewForm.tsx` — bug i18n, dark mode tokens en selects y textarea

### WS-C: Admin analytics & system (commit `6abb9bc`)
**Branch:** `feat/admin-analytics-system`

**Archivos (6):**
- `src/pages/Analytics.tsx` — **+30 ocurrencias dark:** (era 0), `EsqueletoCuadriculaEstadisticas`, `EstadoVacio`
- `src/pages/AuditLogs.tsx` — `<EsqueletoTabla>` en loading, `EstadoVacio icon={FileSearch}`
- `src/pages/BillingDashboard.tsx` — dark mode + empty states + skeletons
- `src/pages/Dashboard.tsx` — copy empty states, dark tokens
- `src/pages/RipsDrafts.tsx` — empty state `FileText`, skeleton, sticky header
- `src/pages/SystemHealth.tsx` — `EsqueletoTarjetaEstadistica`, `EstadoVacio icon={HeartPulse}`

## Integración — Merge a main

Commits mergeados en main:
```
19d61f4 merge: feat/mobile-roles (WS-A) into main
c872dcd merge: feat/admin-core-forms (WS-B) into main
6abb9bc feat(admin-analytics): dark mode + loading skeletons + empty states
58da5ef feat(admin-core): dark mode + empty states + polished forms
44cbe16 merge: feat/foundation-design-system into main
db73b26 feat(foundation): ErrorBoundary global + RealtimeIndicator + iOS zoom-safe inputs
```

## Verificación end-of-phase

| Check | Resultado |
|---|---|
| `npm run build` | ✅ Pasa (warnings de chunk size pre-existentes) |
| `git diff origin/main --stat` | 18 archivos (de 28 en total; resto ya en origin vía WS-C merge) |
| Working tree | ✅ Limpio |
| Touch targets ≥44px mobile | ✅ Aplicado vía `Boton sm = 40px`, `icon = 44px` |
| iOS safe areas | ✅ Aplicado en headers/footers fijos |
| iOS zoom en inputs | ✅ `text-base` explícito en `Entrada`/`Seleccion` |
| Dark mode coherente | ✅ +60 ocurrencias agregadas en Services/Analytics/Users |
| Empty states | ✅ Reemplazados en Doctors/AuditLogs/RipsDrafts/SystemHealth/Services/Reviews |
| Loading skeletons | ✅ Reemplazados `<p>Cargando...>` en AuditLogs/RipsDrafts/Analytics/SystemHealth/PatientHistory |
| Realtime indicator | ✅ `<RealtimeIndicator>` montado globalmente |
| ErrorBoundary | ✅ Captura crashes con fallback accesible |
| Bug i18n en forms | ✅ "El firstName es required" → "El nombre es requerido" en CreateDoctor/CreateService/CreateReview/CreatePayment |
| Validación contraseña | ✅ Unificada a 8 chars en CreateUser y CreateDoctor |
| Spinner custom | ✅ Reemplazado por `Boton isLoading` en CreateReviewForm |
| Bug useMemo en side effect | ✅ MyEarnings DisputeModal usa `useEffect` correctamente |

## Issues identificados para fase futura

1. **Charts compartidos sin dark mode explícito**: `AnalyticsTrendChart.tsx`, `PaymentMethodsChart.tsx`, `RevenueChart.tsx` — necesitan verificación en dark mode.
2. **22+ selects nativos** que aún rompen iOS feel: pendiente migrar a `BottomPicker` en Appointments, AuditLogs, RipsDrafts, Doctors, Users, DoctorAppointments, AppointmentsModal, etc.
3. **Sidebar.tsx icon-buttons `h-9 w-9`**: 36px, < 44px (líneas 317, 408). Documentado para próxima fase.
4. **22+ selects nativos** en mobile contexts que aún no usan `BottomPicker`.
5. **Push notifications**: pendiente integración end-to-end con web-push backend.
6. **Pull-to-refresh**: regla `overscroll-behavior-y: none` removida pero no hay handler custom aún.

## Métricas

- **Tiempo total estimado:** ~60 min (3 workstreams en paralelo + 1 secuencial + integración)
- **Tokens consumidos:** ~280k tokens (modelo MiniMax-M3)
- **Conflictos de merge:** 1 (race condition entre worktrees paralelos, resuelto manualmente con stash + patches)
- **Workstreams exitosos:** 3/3