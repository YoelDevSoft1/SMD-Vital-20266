# Install deps locally without hoisting into parent directories.
# Prevents sub-projects (backend, admin panel) from accidentally resolving
# dependencies from this root Astro installation.
install-strategy=nested